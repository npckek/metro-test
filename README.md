# Metro Test — Полный стек приложения (FastAPI + React)

Полноценное клиент-серверное приложение для работы с данными станций метро: отображение, аналитика и административное управление.

- **backend/** — API на FastAPI + PostgreSQL  
- **frontend/** — пользовательский интерфейс на React + Vite  

## Возможности проекта
### Пользовательская часть
- Просмотр списка станций метро
- Адаптивная сетка карточек с краткой информацией
- Раскрытие подробной информации и статистики по станции
- Работа с геокоординатами, типами транспорта и метаданными

### Администраторская панель
- Авторизация (JWT в HTTP-only cookie)
- Таблица всех станций метро
- CRUD-операции: создание, редактирование, удаление
- Только суперпользователь имеет доступ
- Защита маршрутов на уровне frontend + backend

### Backend
- FastAPI + SQLAlchemy 2.0
- PostgreSQL
- Автоматическая загрузка GeoJSON в БД
- Автоматическое создание суперпользователя
- Чёткая структура проекта
- Документация Swagger автоматически доступна по `/docs`

### Frontend
- React 19 + TypeScript
- TailwindCSS 4
- Vite 7
- Axios, React Router, Radix UI
- Чистая архитектура компонентов

## Требования
- **Docker** ≥ 20.10
- **Docker Compose** ≥ 1.29
- Node.js ≥ 18 
- Python ≥ 3.11

## ⚙️ Установка и запуск проекта
### 🧱 1. Клонирование репозитория
```bash
git clone https://github.com/npckek/metro-test.git
cd metro-test
```
### 2. Запуск backend (FastAPI + PostgreSQL)
- Перейдите в папку:
```bash
cd backend
```
- Создайте файл .env и внесите необходимые данные:
```bash
cp .env.example .env
```
- Запустите сервисы:
```bash
docker compose up --build
```
- Backend будет доступен по адресу:
➡️ http://localhost:8000

- Swagger UI:
➡️ http://localhost:8000/docs

### 3. Запуск frontend (React + Vite)
- Перейдите в папку:
```bash
cd ../frontend
```
- Установите зависимости:
```bash
npm install
```
- Запуск в режиме разработки:
```bash
npm run dev
```

## Отдельные README файлов
- backend/README.md — документация API, запуск Docker
- frontend/README.md — запуск UI, сборка