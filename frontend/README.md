# Frontend — Metro UI (React + Vite + Tailwind)

Интерактивный интерфейс для отображения информации о станциях метро и панели управления администратора.  
Поддерживает авторизацию, CRUD-операции и адаптивный UI.

## Функциональность

### Пользовательская витрина (`/`)
- Адаптивная сетка карточек станций метро
- Возможность раскрыть карточку для просмотра статистики
- Рендеринг данных через API (FastAPI backend)

### Авторизация
- JWT авторизация
- Токен хранится в HTTP-only cookie
- Context API обрабатывает состояние пользователя
- Автоматическое перенаправление при наличии прав доступа

### Панель администратора (`/admin/dashboard`)
- Таблица станций метро
- Создание / обновление / удаление станции
- Модальные окна с формами
- Проверка прав доступа (только суперпользователь)
- Кнопка выхода + переход на витрину

##  Стек технологий

- **React 19**
- **Vite 7**
- **TypeScript 5**
- **TailwindCSS 4**
- **Axios**
- **React Router 7**
- **Radix UI**
- **Lucide Icons**

## Запуск

### 📦 Установка зависимостей
```bash
npm install
```
### Запуск в режиме разработки
```bash
npm run dev
```
Приложение будет доступно по адресу:
http://localhost:5173

### Сборка для продакшена
```bash
npm run build
```
Сборка попадёт в папку:
/dist

## Взаимодействие с backend

- GET /stations/ — список станций
- POST /stations/ — создание (только суперпользователь)
- PUT /stations/{id} — редактирование (только суперпользователь)
- DELETE /stations/{id} — удаление (только суперпользователь)
- POST /auth/token — авторизация
- GET /auth/status — проверка текущего пользователя

## Авторизация и защита страниц

- При входе сохраняется cookie через backend
- При загрузке страницы отправляется запрос /auth/status
- Доступ в /admin/dashboard разрешён только суперпользователю
- При выходе cookie удаляется и контекст очищается

## Линтинг и проверки
```bash
npm run lint
npm run type-check
```